# Progress

Bu fayl status lövhəsidir, changelog deyil (dəyişikliklərin tarixçəsi üçün git-ə bax). Mənalı bir iş bitəndə, ya da scope-a dair qərar veriləndə bu faylı yenilə. Qısa yaz — bir sətir kifayətdir, detal lazımdırsa ADR-ə keçid ver.

## Slice statusu

| Slice | Status | Qeyd |
|---|---|---|
| Layihə scaffold-u | Tətbiq olundu | `pnpm create next-app` — Next.js (App Router, TS, Tailwind, ESLint), bax `docs/decisions/0001-frontend-foundation.md` |
| Slice 1: Auth + Dashboard + Accounts | Tətbiq olundu (+ parol sıfırlama) | Login/Register, `AuthContext`, Dashboard (`GET /net-worth`), Accounts (list/open/archive), Forgot/Reset Password (Resend, bax `financeos-core/docs/decisions/0019-email-infrastructure.md`). Bax `docs/PAGES.md` |
| Slice 2: Ledger | Tətbiq olundu | Əməliyyat siyahısı + gəlir/xərc/transfer/adjust formaları + reconcile. Bax `docs/PAGES.md` |
| Slice 3: Categories | Tətbiq olundu (tam CRUD + iyerarxiya) | Gəlir/Xərc sütunları, hər kateqoriya öz ağac budağında (girintili) göstərilir, redaktə/silmə (4 strategiyalı) hər sətirdə. Sistem kateqoriyası konsepti yoxdur — bax `financeos-core/docs/decisions/0018-per-user-categories.md` |
| Slice 4: Budget | Tətbiq olundu | Şablon/xüsusi büdcə yaratma, prioritet, "Yoxla" (limit/actual/breached), deaktivasiya. Bax `docs/PAGES.md` |
| Slice 5: Goals | Tətbiq olundu | Status filtri, hədəf yaratma (bağlı hesabla), tərəqqi zolağı, tamamla/imtina. Bax `docs/PAGES.md` |
| Slice 6: Settings (FX rates, deaktivasiya/silmə) | Tətbiq olundu | Hesab məlumatı, FX kurs cədvəli+forması, təhlükəli zona (deaktivasiya/silmə). Bax `docs/PAGES.md` |
| Slice 7: Statement Import (bank çıxarışı idxalı) | Tətbiq olundu | Fayl yükləmə → önizləmə/redaktə → təsdiq axını, backend Phase 1-ə (`financeos-core` ADR-0016) UI. Bax `docs/PAGES.md` |
| AI Assistant client | Tətbiq olundu (yalnız "sürətli əlavə" + idxalda AI rozeti) | Dashboard/Ledger-də mətn sahəsi + redaktə olunan təsdiq kartı; idxal icmalında "AI təklifi" rozeti. Tam chat client hələ yoxdur. Bax `financeos-core/docs/decisions/0021-jev-ai-suggestions.md` |

Planlaşdırılmış 7 slice-in hamısı tamamlanıb. Qalan iş: AI Assistant client (ayrı, daha böyük qərar — bax aşağıda) və `financeos-core/docs/PROGRESS.md`-dəki backend-only açıq maddələr (FX cron — xarici provider tələb etdiyi üçün bloklanıb), Statement Import-un XLS/digər bank dəstəyi (backend hələ yalnız Leobank CSV dəstəkləyir).

## Növbəti addımlar

- [x] Next.js layihəsini scaffold et
- [x] Frontend context sənədləri (`CLAUDE.md`, `.claude/rules/`, `docs/decisions/`, `docs/PAGES.md`)
- [x] `src/lib/api/client.ts` — mərkəzləşdirilmiş fetch wrapper (auth header, xəta formatı)
- [x] `src/lib/auth/auth-context.tsx` — token saxlanması, `/auth/me` ilə doğrulama
- [x] Login/Register səhifələri
- [x] `(app)` qorunan layout + nav
- [x] Dashboard səhifəsi (`GET /net-worth`)
- [x] Accounts səhifəsi (list/open/archive)
- [x] Slice 1 yoxlama: `pnpm build`/`lint` təmiz, backend `:3000` + frontend `:3001` paralel işə salınıb, register→open-account→net-worth axını real sorğularla təsdiqləndi (backend cavabları frontend-in `lib/api` tipləri ilə eynidir), CORS preflight yoxlanıldı, test datası silindi
- [ ] Tam brauzer click-through testi (bu sessiyada hər iki slice API-səviyyəli yoxlanıldı, real brauzer sessiyası hələ yoxlanılmayıb)
- [x] Slice 2: Ledger — `lib/api/{ledger,categories}.ts`, gəlir/xərc/transfer/düzəliş formaları, əməliyyat cədvəli, hesab filtri, reconcile düyməsi
- [x] Slice 3: Categories — `createCategory()`, Gəlir/Xərc sütunları, yeni kateqoriya forması, nav-a link əlavə olundu
- [x] Categories: tam CRUD (`updateCategory`/`deleteCategory`), iyerarxik ağac göstərimi (`lib/categories-tree.ts`), idxal icmalında "+ Yeni kateqoriya" modalı — bax `financeos-core/docs/decisions/0018-per-user-categories.md`
- [x] Slice 4: Budget — `lib/api/budget.ts`, şablon-əsaslı/xüsusi büdcə forması (`useFieldArray` ilə allocation sətirləri), büdcə kartları (prioritet, "Yoxla", deaktivasiya)
- [x] Slice 5: Goals — `lib/api/goals.ts`, status filtri, hədəf forması (bağlı hesab istəyə görə), tərəqqi zolağı, tamamla/imtina
- [x] Slice 6: Settings — `lib/api/fx-rates.ts`, `auth.ts`-ə `deactivateAccount`/`deleteAllData`, hesab məlumatı bölməsi, FX kurs cədvəli+forması, təhlükəli zona (parol təsdiqli deaktivasiya/silmə)
- [x] Hər səhifəyə yeni istifadəçilər üçün informativ istifadə qeydi (`InfoNote` komponenti, `src/components/ui/info-note.tsx`) əlavə olundu
- [x] Bütün formalarda input/select-lərə görünən label əlavə olundu (`Field` komponenti, `src/components/ui/field.tsx`)
- [x] Slice 7: Statement Import — `lib/api/statement-import.ts` (`previewStatement`, `commitStatement`), `client.ts`-ə multipart/`FormData` dəstəyi, `/import` səhifəsi (yükləmə forması + `ReviewTable`, `useFieldArray` ilə sətir-sətir kateqoriya/qeyd/daxil-etmə redaktəsi)
- [x] Statement Import: icmal cədvəli eyni təsvirli sətirləri qruplaşdırır (kateqoriya/daxil-etmə bir dəfə qrup üzrə seçilir, "Detallar" ilə fərdi sətirlər açıla bilər), "Gələcək üçün xatırla" seçimi backend-ə `saveRuleKeyword` göndərib istifadəçiyə məxsus kateqoriya qaydası yaradır (bax `financeos-core/docs/decisions/0017-category-rule-personalization.md`)
- [ ] Statement Import: XLS dəstəyi, Leobank-dan başqa bank profilləri (hər ikisi backend-in əhatəsini gözləyir)
- [x] Parol sıfırlama: `lib/api/auth.ts`-ə `forgotPassword`/`resetPassword`, `/forgot-password` və `/reset-password` səhifələri, login-ə "Şifrəni unutmusunuz?" linki — bax `financeos-core/docs/decisions/0019-email-infrastructure.md`
- [ ] AI təklifləri — web hissəsi (bax `financeos-core/docs/decisions/0021-jev-ai-suggestions.md`; backend fazaları `financeos-core/docs/PROGRESS.md`-dədir):
  - [x] Faza 1b: idxal icmalında AI təklifli qruplara "AI təklifi" rozeti (etibar faizi tooltip-də), `suggestionSource`/`suggestionConfidence` tipləri (`lib/api/statement-import.ts`), "Gələcək üçün xatırla" AI təkliflərində də görünür (default söndürülü). `pnpm build`/`lint` təmiz. Real brauzer click-through hələ edilməyib
  - [x] Faza 2d: `src/components/quick-add/{quick-add-bar,proposal-card}.tsx` (Dashboard + Ledger) — mətn → `POST /assistant/parse-transaction` → redaktə olunan təsdiq kartı (boş sahələr vurğulanır, valyuta uyğunsuzluğu xəbərdarlığı) → mövcud `recordIncome`/`recordExpense` (`occurredAt` əlavə olundu); `dayOffset` `lib/local-date.ts` ilə yerli tarixə çevrilir. `pnpm build`/`lint` təmiz. Real brauzer click-through hələ edilməyib
  - [x] `docs/PAGES.md`-ə yeni endpoint-lər
- [ ] AI Assistant: tam chat client (çox addımlı dialoq, streaming) — sürətli əlavədən sonra ayrıca qərar

## Log

*(ən yenisi əvvəldə — sessiya/qərar başına bir sətir, aidiyyatı olan ADR-ə keçid ver)*

- Bank idxalı (`/import`): hər qrup üçün "Köçürmə (hesablar arası)" seçimi (eyni valyutalı hesab, `transferAccountId`), credit qrupunda xərc kateqoriyası seçimi (geri qaytarma), "Gələcək üçün xatırla" hamısı üçün default seçili; core-da `commit` 413 səhvi düzəldildi (bax `financeos-core/docs/decisions/0022-import-transfers-and-refunds.md`). `pnpm build`/`lint` təmiz, brauzer klik-testi edilməyib.

- AI təklifləri web tərəfində tətbiq olundu (backend `financeos-core/docs/decisions/0021-jev-ai-suggestions.md`-də): idxal icmalında "AI təklifi" rozeti və Dashboard/Ledger-də "sürətli əlavə" (`components/quick-add/`) — mətn → təklif → redaktə olunan təsdiq kartı → mövcud `recordIncome`/`recordExpense`. AI heç vaxt birbaşa yazmır. `lib/api/ledger.ts`-ə `occurredAt` əlavə olundu, `lib/local-date.ts` `dayOffset`-i yerli tarixə çevirir. `<select>` disabled placeholder-i brauzerdə avtomatik ilk seçimə düşdüyü üçün istiqamət sahəsi `''` default ilə saxlanılır. `pnpm build`/`lint` təmiz; backend real DB ilə canlı yoxlanıldı, real brauzer click-through və real AI API-ləri ilə test hələ edilməyib.

- Parol sıfırlama tətbiq olundu (backend tərəfi `financeos-core/docs/decisions/0019-email-infrastructure.md`-də, Resend ilə): `lib/api/auth.ts`-ə token tələb etməyən `forgotPassword`/`resetPassword`. Yeni `/forgot-password` (email forması, nəticə email mövcud olsun-olmasın eyni mesajdır — backend-in sızdırmama davranışına uyğun) və `/reset-password?token=...` (`useSearchParams`-ın layihədə ilk istifadəsi, Next.js-in tələbinə görə `<Suspense>` daxilində; yeni parol + təkrar sahəsi, zod `.refine` uyğunluq üçün) səhifələri — hər ikisi `(app)` xaricində, login/register-in eyni "sadə async submit" konvensiyası ilə (`useMutation` yox). `login/page.tsx`-ə "Şifrəni unutmusunuz?" linki əlavə olundu. `pnpm build`/`lint` təmiz.
- Manual testdə aşkarlanan problemlərə cavab olaraq Categories tam CRUD-a qədər genişləndirildi (backend tərəfi `financeos-core/docs/decisions/0018-per-user-categories.md`-də): `lib/api/categories.ts`-ə `updateCategory`/`deleteCategory` (+ `DELETE_CATEGORY_STRATEGIES`) əlavə olundu, `lib/api/client.ts`-in `apiFetch`-i `PATCH`/`DELETE` metodlarını dəstəkləyəcək şəkildə genişləndirildi. Yeni `lib/categories-tree.ts` (`buildCategoryTree`/`flattenCategoryTree`/`indentLabel`/`collectDescendantIds`) — render qatında sırf hesablama, backend-in flat siyahısına toxunmur. `/categories` səhifəsi rekursiv `CategoryTreeRow` (`_components/category-tree-row.tsx`) ilə yenidən quruldu: hər sətir girintili (`depth * 16px`), "Redaktə" (ad/ikon/valideyn, inline forma) və "Sil" (4 strategiya: köçür/kateqoriyasız et/sil/arxivlə — reassign seçiləndə hədəf kateqoriya `<select>`-i görünür) düymələri. Yeni `components/ui/modal.tsx` (layihədə əvvəllər heç bir modal komponenti yox idi, sıfırdan — backdrop+ESC) və `import/_components/create-category-modal.tsx` — idxal icmalının hər qrup sətirində kateqoriya `<select>`-inin yanına "+ Yeni" düyməsi əlavə olundu, yaradılan kateqoriya avtomatik qrupa tətbiq olunur (`['categories']` invalidasiyası ilə). `pnpm build`/`lint` təmiz. Backend `financeos-core` tərəfində: sistem kateqoriyası konsepti ləğv olundu (qeydiyyatda hər istifadəçiyə öz nümunə kateqoriyaları klonlanır), `PATCH`/`DELETE /categories/:id`, yeni `ledger_entries.archived_at` sütunu — 51 backend e2e test yaşıl, canlı serverə qarşı curl ilə redaktə+bütün 4 silmə strategiyası təsdiqləndi (ayrıca təcrid olunmuş test instansında — əsas dev server köhnə Prisma Client ilə yaddaşda qaldığı üçün restart tələb edir).
- Statement Import icmal ekranı qruplaşdırma ilə genişləndirildi: `review-table.tsx`-də `preview.rows` `(direction, təsvir)` üzrə qruplaşdırılır (`buildGroups`, sətir sayına görə azalan sırada), hər qrup üçün bir kateqoriya `<select>` (dəyişdikdə qrupdakı bütün sətirlərin `categoryId`-i `setValue` ilə yenilənir) və bir "daxil et" checkbox-u (qarışıq seçim üçün `indeterminate`), "Detallar" ilə fərdi sətirlər (tarix/qeyd/dublikat bayrağı) açılıb fərdi düzəliş edilə bilər. Alt qat (`fingerprint`/`categoryId`/`note`/`include` sahələri) dəyişmədi — qruplaşdırma sırf render qatındadır. Təklif olunmayan qruplarda görünən "Gələcək üçün xatırla" checkbox-u commit zamanı qrupun ilk sətrinə `saveRuleKeyword` əlavə edir (`financeos-core`-un yeni istifadəçiyə-məxsus `category_suggestion_rules` dəstəyi, bax `financeos-core/docs/decisions/0017-category-rule-personalization.md`). `pnpm build`/`lint` təmiz. Backend+frontend ilə real axın curl vasitəsilə yoxlanıldı: naməlum tacir → commit `saveRuleKeyword` ilə → eyni tacirin yeni sətri artıq avtomatik kateqoriya təklif edir → başqa istifadəçi bu qaydanı görmür (izolyasiya) → GDPR data-silmə qaydanı tam silir (qlobala orphan etmir — bu, backend tərəfində əl-ilə sınaq zamanı tapılan ayrıca bug idi, bax `financeos-core/docs/PROGRESS.md`).
- Slice 7 (Statement Import) tətbiq olundu: `lib/api/statement-import.ts` (`previewStatement` — multipart `FormData`, `commitStatement`), `lib/api/client.ts`-dəki `apiFetch` `FormData` body-lərini tanıyacaq şəkildə genişləndirildi (bu halda `Content-Type` header-i qoyulmur, brauzer öz boundary-sini özü əlavə edir). `/import` səhifəsi: hesab/bank/fayl seçimi → `POST /statement-import/preview` → `ReviewTable` (`_components/review-table.tsx`, `react-hook-form`+`useFieldArray`, Budget səhifəsindəki eyni pattern) sətir-sətir daxil-etmə checkbox-u (default: dublikat olmayanlar seçili), kateqoriya seçimi (`suggestedCategoryId` ilə prefill, istiqamətə görə filtrlənmiş), qeyd sahəsi (default təsvir), dublikat/daxili-köçürmə/balans-uyğunsuzluq rozetləri → `POST /statement-import/commit` → nəticə (`imported`/`skippedDuplicates`/`excluded`) göstərilir, `['ledger-entries']`/`['accounts']`/`['net-worth']` invalidasiya olunur. Nav-a "İdxal" linki əlavə olundu. `pnpm build`/`lint` təmiz. Backend+frontend paralel işə salınıb, real Leobank CSV nümunəsi (62 sətir) ilə UI-nin göndərdiyi eyni request formatları curl vasitəsilə tam axın (preview → commit → `GET /ledger/entries` 62 sətir təsdiqlədi → eyni faylın təkrar preview-i 62/62 dublikat aşkarladı) yoxlanıldı, test datası silindi. Qeyd: bu, əvvəlki bütün slice-lər kimi API-səviyyəli yoxlamadır — tam brauzer click-through (fayl seçici, cədvəl render-i) hələ edilməyib.
- Bütün formalardakı input/select-lərə görünən label əlavə olundu — əvvəllər yalnız `placeholder` var idi, bu, dəyər daxil edildikdən sonra sahənin nə olduğunu anlamağı çətinləşdirirdi. Ortaq `Field` komponenti (`src/components/ui/field.tsx`, native `<label>` ilə örtür, `htmlFor`/`id` lazım deyil) yaradıldı və login/register/accounts/ledger(3 forma)/categories/budget/goals/settings(2 komponent) formalarının hamısına tətbiq edildi. `pnpm build`/`lint` təmiz, login/register-in statik render-də label-lərin göründüyü curl ilə yoxlanıldı.
- Hər 9 səhifəyə (login, register, dashboard, accounts, ledger, categories, budget, goals, settings) yeni istifadəçilər üçün qısa, informativ istifadə qeydi əlavə olundu — ortaq `InfoNote` komponenti (`src/components/ui/info-note.tsx`), hər səhifənin başlığı altında. `pnpm build`/`lint` təmiz, mətnlərin görünməsi production build-də (`pnpm start`) curl ilə yoxlanıldı.
- Slice 6 (Settings) tətbiq olundu — planlaşdırılmış 6 slice-in sonuncusu: `lib/api/fx-rates.ts` (`listFxRates`, `upsertFxRate`), `lib/api/auth.ts`-ə `deactivateAccount`/`deleteAllData` əlavə olundu. `/settings` səhifəsi: hesab məlumatı (statik, `AuthContext`-dən), FX kursları (cədvəl+əlavə/yeniləmə forması), Təhlükəli Zona (deaktivasiya və data silinməsi — hər ikisi parol input-u + `window.confirm` ilə iki səviyyəli təsdiq, uğur olduqda sessiya təmizlənib `/login`-ə yönləndirilir). Nav-dan "tezliklə" placeholder məntiqi (bütün linklər indi aktiv olduğu üçün) silindi. `pnpm build`/`lint` təmiz. Backend+frontend paralel, FX kurs əlavəsi+siyahısı, yanlış/düzgün parolla deaktivasiya (401→200→sonrakı sorğular 401) və ayrıca istifadəçi ilə delete-data (401→200→401) axınları curl ilə yoxlanıldı; qlobal FX test sətri (`EUR/AZN`, `delete-data` ilə silinmir, çünki istifadəçiyə bağlı deyil) əl ilə `psql`-lə təmizləndi.
- Slice 5 (Goals) tətbiq olundu: `lib/api/goals.ts` (`createGoal`, `listGoals`, `completeGoal`, `abandonGoal`). `/goals` səhifəsi: status tab-ları (Hamısı/Aktiv/Tamamlanıb/İmtina, server-side `?status=` filtri), "Yeni hədəf" forması (valyuta default `user.baseCurrency`, bağlı hesab istəyə görə), hədəf kartları (tərəqqi zolağı `progress.percent` varsa, yalnız aktiv hədəflərdə Tamamla/İmtina düymələri). Nav-a "Hədəflər" linki aktivləşdirildi. `pnpm build`/`lint` təmiz. Backend+frontend paralel, bağlı hesabla hədəf yaratma → progress 0%-dən 25%-ə (gəlir yazıldıqdan sonra) → status filtri → tamamlama axını curl ilə yoxlanıldı, test datası silindi.
- Slice 4 (Budget) tətbiq olundu: `lib/api/budget.ts` (`getTemplates`, `createBudget`, `listBudgets`, `checkBudget`, `updateBudgetPriority`, `deactivateBudget`). `/budget` səhifəsi: "Yeni büdcə" forması (şablon seçəndə `useFieldArray` ilə allocation sətirləri şablonun bucket-lərindən prefill olunur, kateqoriya hər sətirdə əl ilə seçilir; "Xüsusi"-də sərbəst sətir əlavə/sil), büdcə kartları (allocation-lar, prioritet dəyişmə, "Yoxla" düyməsi limit/actual/breached göstərir, deaktivasiya). Percent/priority sahələri `z.coerce.number()` əvəzinə string+regex saxlanıldı (react-hook-form+zodResolver-in input/output tip uyğunsuzluğu problemi, layihənin digər formalarındakı eyni pattern). `pnpm build`/`lint` təmiz. Backend+frontend paralel, tam axın (şablonlar → xərc kateqoriyalı büdcə yarat → gəlir+xərc qeyd et → limit aşımı düzgün aşkarlandı → prioritet dəyiş → deaktiv et) curl ilə yoxlanıldı, test datası silindi.
- Slice 3 (Categories) tətbiq olundu: `lib/api/categories.ts`-ə `createCategory()` əlavə olundu. `/categories` səhifəsi: Gəlir/Xərc iki sütun (sistem kateqoriyalar "sistem" etiketi ilə), "Yeni kateqoriya" forması (ad, kind, valideyn seçimi — yalnız seçilmiş kind-dəki mövcud kateqoriyalar, `useWatch` ilə reaktiv filtrlənir, icon istəyə görə). Nav-a "Kateqoriyalar" linki əlavə olundu. `pnpm build`/`lint` təmiz. Backend+frontend paralel, seed edilmiş 8 sistem kateqoriyası + valideynli yeni kateqoriya yaratma + uyğunsuz `kind`-lə valideyn seçəndə 400 gözləndiyi kimi curl ilə yoxlanıldı, test datası silindi.
- Slice 2 (Ledger) tətbiq olundu: `lib/api/ledger.ts` (`listEntries`, `recordIncome`, `recordExpense`, `transfer`, `adjustBalance`, `reconcile`), `lib/api/categories.ts` (`listCategories`). `/ledger` səhifəsi: hesab filtri, 4 tab-lı forma (Gəlir/Xərc — ortaq `EntryForm`, kateqoriya `kind`-ə görə filtrlənir; Transfer; Düzəliş), əməliyyat cədvəli (hesab/kateqoriya adları `accounts`/`categories` siyahıları ilə join edilir), "Balansı yenidən hesabla" düyməsi. Uğurlu mutasiyalardan sonra `['ledger-entries']`+`['accounts']`+`['net-worth']` invalidasiya olunur. `pnpm build`/`lint` təmiz. Backend+frontend paralel işə salınıb, iki hesab + gəlir/xərc(kateqoriyalı)/transfer/adjust/reconcile tam axını curl ilə yoxlanıldı, balanslar gözlənilən qiymətlərlə üst-üstə düşdü, test datası silindi.
- Slice 1 tətbiq olundu: `lib/api/{client,auth,accounts,net-worth}.ts` (backend DTO-larının əl ilə güzgüsü, bax `docs/decisions/0001-frontend-foundation.md`), `AuthContext` (localStorage token + `/auth/me` doğrulaması, 401-də avtomatik logout), Login/Register formaları (`react-hook-form`+`zod`), qorunan `(app)` shell + nav, Dashboard (`GET /net-worth`), Accounts (list/open/archive, TanStack Query ilə invalidasiya). `pnpm build`/`lint` təmiz. Backend+frontend paralel işə salınıb, real register→open-account→net-worth axını curl ilə (frontend-in çağırdığı eyni endpoint/format) yoxlanıldı, CORS preflight təsdiqləndi, test datası `POST /auth/delete-data` ilə təmizləndi.
- Layihə scaffold edildi (Next.js 16, App Router, TS, Tailwind, ESLint, pnpm), frontend context sənədləri yaradıldı: `CLAUDE.md`, `.claude/rules/api-integration.md`, `.claude/rules/ui-conventions.md`, `docs/decisions/0001-frontend-foundation.md`, `docs/PAGES.md`. Backend-də bu seçimin tələb etdiyi yeganə dəyişiklik (CORS) `financeos-core`-un öz `docs/decisions/0014-cors-for-web-client.md`-də sənədləşdirilib.
